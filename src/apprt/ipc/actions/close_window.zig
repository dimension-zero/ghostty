//! IPC action handler for closing a window.
//!
//! Closes the currently focused window.

const std = @import("std");
const protocol = @import("../protocol.zig");

// No parameters needed - closes the focused window
